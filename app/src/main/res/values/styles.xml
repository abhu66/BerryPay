<resources>

    <!-- Base application theme. -->
    <style name="LoginActivityTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">#ff1919</item>
        <item name="colorPrimaryDark">#ff1919</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>
    <style name="LoginActivityTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="LoginActivityTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>
